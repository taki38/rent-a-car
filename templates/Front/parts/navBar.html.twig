{# <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('dashboard') }}">Rent a Car</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('dashboard') }}">Home
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Voitures</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Utilitaires</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Nos services</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Infos pratiques</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item text-dark" href="#">Conseils location</a>
                        <a class="dropdown-item text-dark" href="#">Jeunes Conducteurs</a>
                        <a class="dropdown-item text-dark" href="#">Service Sans Permis</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item text-dark" href="#">Contactez-Nous</a>
                    </div>
                </li>
            </ul>
            {% if is_granted('ROLE_USER') %}
                <form class="d-flex">



                    <div class="dropdown">
                        <button class="btn btn-light dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                            Bonjour {{ app.user.firstname }}
                        </button>
                        <ul class="dropdown-menu " aria-labelledby="dropdownMenuButton1">
                            <li><a href="{{ path('dashboard') }}" class=" dropdown-item text-dark" role="button">Mon compte</a></li>
                            <li><a href="{{ path('app_logout') }}" class=" dropdown-item text-dark" role="button">Me déconnecter</a></li>
                            <li><a class="dropdown-item" href="#">Something else here</a></li>
                        </ul>
                    </div>
                </form>
            {% else %}
                <form class="d-flex">
                    <a href="{{ path('app_login') }}" class="btn btn-light" role="button">Me connecter</a>
                </form>
            {% endif %}
        </div>
    </div>
</nav>
    #}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container px-5">
        <a class="navbar-brand" href="{{ path('dashboard') }}">Rent a Car</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item"><a class="nav-link " href="{{ path('dashboard') }}">Accueil</a></li>
                <li class="nav-item"><a class="nav-link " href="{{ path('dashboard') }}">Voitures</a></li>
                <li class="nav-item"><a class="nav-link " href="{{ path('dashboard') }}">Utilitaires</a></li>
                <li class="nav-item"><a class="nav-link " href="{{ path('dashboard') }}">Nos services</a></li>
                <li class="nav-item"><a class="nav-link " href="{{ path('dashboard') }}">FAQ</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" id="navbarDropdownPortfolio" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">{{ app.user.firstname }}</a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownPortfolio">
                            <li><a class="dropdown-item" href="{{ path('dashboard') }}">Espace Admin</a></li>
                            <li><a class="dropdown-item" href="">Mon compte</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Me déconnecter</a></li>
                        </ul>
                    </li>
                {% elseif is_granted('ROLE_USER') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdownPortfolio" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">{{ app.user.firstname }}</a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownPortfolio">
                        <li><a class="dropdown-item" href="">Mon compte</a></li>
                        <li><a class="dropdown-item" href="{{ path('car_add') }}">Ajouter une voiture</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Me déconnecter</a></li>
                    </ul>
                </li>
                {% endif %}
                {% if is_granted('IS_ANONYMOUS') %}
                    <li class="nav-item"><a class="nav-link "  href="{{ path('app_login') }}">Me connecter</a></li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>